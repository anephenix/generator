// import { createDirectory } from '../src';

describe('createDirectory', () => {

  describe('before running the action', () => {
    test.todo('should check if the base path exists');
    test.todo('should check if the folder already exists');
    test.todo('should check if it has permission to create the folder');
  });

  describe('after running the action', () => {
    test.todo('should check that the folder now exists');
  });

});

describe('createFile', () => {

  describe('before running the action', () => {
    test.todo('should check if the base path exists');
    test.todo('should check if it has permission to create the file');
    test.todo('should check if the file already exists');
  });

  describe('after running the action', () => {
    test.todo('should check that the file now exists');
  });

});

describe('install dependency', () => {
  
    describe('before running the action', () => {
      test.todo('should check if the base path exists');
      test.todo('should check if the dependency is already installed');
      test.todo('should check if it has permission to install the dependency');
    });
  
    describe('after running the action', () => {
      test.todo('should check that the dependency is installed');
    });
  
});