// Cucumber hooks file generated by @anephenix/generator
// Dependencies
const { After, BeforeAll, AfterAll } = require('@cucumber/cucumber');
// const scope = require('./support/scope');

BeforeAll({ timeout: 20000 }, async () => {});

After(async () => {});

AfterAll(async () => {});
